# notes

Hand-edited sprint log. Edit when state changes; remove items that go stale.
Do not paste generated content from `.lake/build/blueprint/` or `blueprint/web/` here.

## Active sprint

LinearSystems milestone landed: `observabilityMatrix`, `controllabilityMatrix`,
and the bridge theorem `isObservable_iff_observabilityMatrix_ker_trivial`.
`lake build` green.

Repo flattened: lake project is now at the git root (was nested under
`LeanForControl/LeanForControl/`). Standard Lean+blueprint layout.

Reporting infrastructure being stood up: `LeanArchitect` wired into
`lakefile.toml`; `leanblueprint` scaffold under `blueprint/` pending
interactive `leanblueprint new`; `@[blueprint "..."]` attributes still TODO
for the seven LinearSystems declarations.

## Open follow-ups

- Run `leanblueprint new` from the git root (now possible after the flatten)
  and customize `blueprint/src/content.tex` with the seven LinearSystems nodes.
- Hautus track helpers: rank ↔ trivial-kernel bridge, block-matrix rank lemmas,
  `A`-invariant subspace extraction (Cayley–Hamilton style), and
  `(C * A^k) *ᵥ x` ↔ `C *ᵥ (A^k *ᵥ x)` coercions.
  See the tail comment in `LeanForControl/LinearSystems/Observability.lean`.
- Promote `LeanArchitect` `rev` from `main` to a pinned commit once the
  blueprint build is reproducible end to end.
- Decide whether to extend `.github/workflows/lean_action_ci.yml` to deploy
  `blueprint/web/` to GitHub Pages. Currently local-only.

## Conventions

- This file is hand-edited. The blueprint LaTeX nodes under
  `.lake/build/blueprint/` are generated by `lake build :blueprint` and must
  not be hand-edited.
- End-of-session workflow: `lake build` → update this file →
  `lake build :blueprint` → `leanblueprint checkdecls` → `leanblueprint web`.
- `leanblueprint` lives in conda env `cortese`. Activate before running it.
